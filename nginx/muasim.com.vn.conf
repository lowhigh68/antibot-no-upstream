# ============================================================
# AUTO-GENERATED by da_to_openresty.sh
# User     : muasim
# Domain   : muasim.com.vn
# Generated: 2026-04-14 00:04:12
# DO NOT EDIT — regenerated on each DA event
# ============================================================

# ── HTTP ─────────────────────────────────────────────────────
server {
    listen 0.0.0.0:80;

    server_name muasim.com.vn www.muasim.com.vn;

    access_log /var/log/nginx/domains/muasim.com.vn.log antibot_fmt;
    access_log /var/log/nginx/domains/muasim.com.vn.bytes bytes;
    error_log  /var/log/nginx/domains/muasim.com.vn.error.log;

    root  "/home/muasim/domains/muasim.com.vn/public_html";
    index index.php index.html index.htm;

    set $antibot_debug "1";
    access_by_lua_block {
        require("antibot").run()
    }
    log_by_lua_block { require("antibot").log() }
    header_filter_by_lua_block {
        local ctx = ngx.ctx.antibot
        if not ctx or not ctx.inject_candidate then return end
        local ct = ngx.header["Content-Type"] or ""
        if not ct:find("text/html", 1, true) then return end
        ctx.browser_needed        = true
        ngx.header.content_length = nil
    }
    body_filter_by_lua_block {
        require("antibot.detection.browser.inject").filter()
    }

    location = /antibot/verify {
        access_by_lua_block { return; }
        content_by_lua_block {
            local v = require "antibot.enforcement.challenge.verify_token"
            v.handle()
        }
    }
    location = /antibot/beacon {
        access_by_lua_block { return; }
        content_by_lua_block {
            require("antibot.detection.browser.beacon_handler").handle()
        }
    }
    location = /antibot/restore {
        access_by_lua_block { return; }
        content_by_lua_block {
            require("antibot.enforcement.challenge.restore_token").handle()
        }
    }
    location = /antibot/debug {
        access_by_lua_block {
            if ngx.var.remote_addr ~= "127.0.0.1" then
                ngx.exit(403); return
            end
        }
        content_by_lua_block {
            local e = require "antibot.enforcement.decision.engine"
            ngx.header["Content-Type"] = "application/json"
            local t = e.thresholds()
            ngx.say(string.format(
                '{"version":"4.3.5","monitor":%d,"challenge":%d,"block":%d}',
                t.monitor, t.challenge, t.block))
        }
    }

    location = "/.well-known/autoconfig/mail/config-v1.1.xml" {
        proxy_pass http://unix:/usr/local/directadmin/shared/internal.sock;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
    }

    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot|map|mp4|pdf)$ {
        access_by_lua_block { return }
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        proxy_pass      http://apache_http;
        proxy_set_header Host            $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }


    location / {
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        proxy_pass      http://apache_http;
        proxy_set_header X-Client-IP       $remote_addr;
        proxy_set_header X-Accel-Internal  /nginx_static_files;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host  $host;
        proxy_set_header Accept-Encoding   "";
        proxy_hide_header Upgrade;
    }

    location /nginx_static_files/ {
        alias    "/home/muasim/domains/muasim.com.vn/public_html/";
        internal;
    }

    include /etc/nginx/webapps.conf;
}

# ── HTTPS ────────────────────────────────────────────────────
server {
    listen 0.0.0.0:443 ssl;
    http2  on;

    server_name muasim.com.vn www.muasim.com.vn;

    ssl_certificate     /usr/local/directadmin/data/users/muasim/domains/muasim.com.vn.cert.combined;
    ssl_certificate_key /usr/local/directadmin/data/users/muasim/domains/muasim.com.vn.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    access_log /var/log/nginx/domains/muasim.com.vn.log antibot_fmt;
    access_log /var/log/nginx/domains/muasim.com.vn.bytes bytes;
    error_log  /var/log/nginx/domains/muasim.com.vn.error.log;

    root  "/home/muasim/domains/muasim.com.vn/public_html";
    index index.php index.html index.htm;

    ssl_client_hello_by_lua_block {
        require("antibot.transport.tls.ja3").capture()
    }
    ssl_certificate_by_lua_block {
        require("antibot.transport.tls.ja3s").capture()
    }

    set $antibot_debug "1";
    access_by_lua_block {
        require("antibot").run()
    }
    log_by_lua_block { require("antibot").log() }
    header_filter_by_lua_block {
        local ctx = ngx.ctx.antibot
        if not ctx or not ctx.inject_candidate then return end
        local ct = ngx.header["Content-Type"] or ""
        if not ct:find("text/html", 1, true) then return end
        ctx.browser_needed        = true
        ngx.header.content_length = nil
    }
    body_filter_by_lua_block {
        require("antibot.detection.browser.inject").filter()
    }

    location = /antibot/verify {
        access_by_lua_block { return; }
        content_by_lua_block {
            local v = require "antibot.enforcement.challenge.verify_token"
            v.handle()
        }
    }
    location = /antibot/beacon {
        access_by_lua_block { return; }
        content_by_lua_block {
            require("antibot.detection.browser.beacon_handler").handle()
        }
    }
    location = /antibot/restore {
        access_by_lua_block { return; }
        content_by_lua_block {
            require("antibot.enforcement.challenge.restore_token").handle()
        }
    }
    location = /antibot/debug {
        access_by_lua_block {
            if ngx.var.remote_addr ~= "127.0.0.1" then
                ngx.exit(403); return
            end
        }
        content_by_lua_block {
            local e = require "antibot.enforcement.decision.engine"
            ngx.header["Content-Type"] = "application/json"
            local t = e.thresholds()
            ngx.say(string.format(
                '{"version":"4.3.5","monitor":%d,"challenge":%d,"block":%d}',
                t.monitor, t.challenge, t.block))
        }
    }

    location = "/.well-known/autoconfig/mail/config-v1.1.xml" {
        proxy_pass http://unix:/usr/local/directadmin/shared/internal.sock;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
    }

    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot|map|mp4|pdf)$ {
        access_by_lua_block { return }
        proxy_buffering       off;
        proxy_next_upstream    error timeout invalid_header;
        proxy_next_upstream_tries 2;
        proxy_http_version    1.1;
        proxy_set_header      Connection "";
        proxy_pass            https://apache_https;
        proxy_set_header      Host            $host;
        proxy_set_header      X-Forwarded-For $remote_addr;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }


    location / {
        proxy_buffering       off;
        proxy_next_upstream    error timeout invalid_header;
        proxy_next_upstream_tries 2;
        proxy_http_version    1.1;
        proxy_set_header      Connection "";
        proxy_pass            https://apache_https;
        proxy_ssl_server_name on;
        proxy_ssl_name        $host;
        proxy_ssl_verify      off;
        proxy_set_header      X-Client-IP       $remote_addr;
        proxy_set_header      X-Accel-Internal  /nginx_static_files;
        proxy_set_header      Host              $host;
        proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header      X-Forwarded-Proto https;
        proxy_set_header      X-Forwarded-Host  $host;
        proxy_hide_header     Upgrade;
    }

    location /nginx_static_files/ {
        alias    "/home/muasim/domains/muasim.com.vn/public_html/";
        internal;
    }

    include /etc/nginx/webapps.ssl.conf;
}
